#!/bin/bash
# minecraft edit script

WALLS=$GAMEDIR/walls
PORT=25565
MULTI=false
NAMES=(server ops bans)
FILES=($WALLS/server.properties $WALLS/ops.txt $WALLS/banned-players.txt)
UPDATE="$WALLS/update.sh"
